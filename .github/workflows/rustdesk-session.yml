name: 🦀 RustDesk Mac Session

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Duração da sessão (horas)'
        required: true
        default: '2'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
          - '4'
          - '5'
          - '6'
      runner_size:
        description: 'Tamanho do runner (standard=Free/Pro, large/xlarge=Team/Enterprise)'
        required: true
        default: 'standard'
        type: choice
        options:
          - 'standard'
          - 'large'
          - 'xlarge'

env:
  SESSION_DURATION: ${{ github.event.inputs.duration }}

jobs:
  mac-rustdesk-session:
    name: 🦀 Mac RustDesk Session
    runs-on: ${{ github.event.inputs.runner_size == 'xlarge' && 'macos-14-xlarge' || github.event.inputs.runner_size == 'large' && 'macos-14-large' || 'macos-14' }}
    timeout-minutes: 370
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📊 Informações do Sistema
        run: |
          chmod +x scripts/system-info.sh
          ./scripts/system-info.sh

      - name: 🦀 Configurar RustDesk
        id: rustdesk
        run: |
          chmod +x scripts/setup-rustdesk.sh
          
          # Gerar senha segura
          RUSTDESK_PASSWORD=$(openssl rand -base64 12 | tr -dc 'a-zA-Z0-9' | head -c 12)
          
          # Mascarar a senha nos logs do GitHub Actions
          echo "::add-mask::${RUSTDESK_PASSWORD}"
          
          # Salvar como variável de ambiente (mascarada)
          echo "RUSTDESK_PASSWORD=${RUSTDESK_PASSWORD}" >> $GITHUB_ENV
          
          # Exportar e executar
          export RUSTDESK_PASSWORD
          ./scripts/setup-rustdesk.sh
          
          # Aguardar RustDesk registrar nos servidores relay
          sleep 10

      - name: 🔑 Obter RustDesk ID
        id: get_id
        run: |
          # Aguardar inicialização completa
          sleep 5
          
          RUSTDESK_CLI="/Applications/RustDesk.app/Contents/MacOS/rustdesk"
          
          if [ -f "$RUSTDESK_CLI" ]; then
            # Tentar obter ID
            RUSTDESK_ID=$("$RUSTDESK_CLI" --get-id 2>/dev/null || echo "")
            
            if [ -z "$RUSTDESK_ID" ]; then
              # Tentar do arquivo de config
              CONFIG_DIR="$HOME/Library/Application Support/RustDesk"
              RUSTDESK_ID=$(cat "${CONFIG_DIR}/id" 2>/dev/null || echo "")
            fi
            
            if [ -n "$RUSTDESK_ID" ]; then
              echo "RUSTDESK_ID=${RUSTDESK_ID}" >> $GITHUB_ENV
              echo "✅ RustDesk ID: ${RUSTDESK_ID}"
            else
              echo "⚠️ Não foi possível obter o RustDesk ID automaticamente"
              echo "RUSTDESK_ID=Ver_Logs" >> $GITHUB_ENV
            fi
          fi

      - name: 💾 Salvar Credenciais (Arquivo Privado)
        run: |
          mkdir -p credentials
          
          # Criar arquivo com credenciais (NÃO exibir no log)
          cat > credentials/connection-info.txt << EOF
          ════════════════════════════════════════════════════════════
                    GITHUB MAC REMOTE - CREDENCIAIS
          ════════════════════════════════════════════════════════════
          
          Iniciado por: ${{ github.actor }}
          Data/Hora: $(date -u '+%Y-%m-%d %H:%M:%S UTC')
          Run ID: ${{ github.run_id }}
          
          ────────────────────────────────────────────────────────────
          Credenciais RustDesk:
          ────────────────────────────────────────────────────────────
          
            ID:    ${{ env.RUSTDESK_ID }}
            Senha: ${{ env.RUSTDESK_PASSWORD }}
          
          ────────────────────────────────────────────────────────────
          Como Conectar:
          ────────────────────────────────────────────────────────────
          
          1. Baixe o RustDesk em: https://rustdesk.com/download
          2. Instale e abra o RustDesk no seu computador
          3. Digite o ID mostrado acima
          4. Digite a senha mostrada acima
          5. Clique em Connect!
          
          ════════════════════════════════════════════════════════════
          ⚠️  ATENÇÃO: Este arquivo contém credenciais sensíveis.
              Não compartilhe com terceiros.
          ════════════════════════════════════════════════════════════
          EOF

      - name: 📦 Upload Artifact de Credenciais
        uses: actions/upload-artifact@v4
        with:
          # Nome do artifact inclui o usuário que iniciou para identificação
          name: credentials-${{ github.actor }}-${{ github.run_id }}
          path: credentials/
          retention-days: 1
          # Artifact é privado por padrão - apenas quem tem acesso ao repo pode baixar

      - name: 📋 Instruções de Conexão
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════════════╗"
          echo "║              🦀 GITHUB MAC REMOTE - PRONTO                       ║"
          echo "╚══════════════════════════════════════════════════════════════════╝"
          echo ""
          echo "👤 Sessão iniciada por: ${{ github.actor }}"
          echo ""
          echo "🔑 RustDesk ID: ${{ env.RUSTDESK_ID }}"
          echo ""
          echo "🔐 Senha: ████████████ (protegida)"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📥 COMO OBTER A SENHA:"
          echo ""
          echo "   1. Vá para a aba 'Summary' deste workflow"
          echo "   2. Baixe o artifact: credentials-${{ github.actor }}-${{ github.run_id }}"
          echo "   3. Abra o arquivo connection-info.txt"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📖 Como conectar:"
          echo ""
          echo "   1. Baixe o RustDesk: https://rustdesk.com/download"
          echo "   2. Abra o RustDesk e digite o ID: ${{ env.RUSTDESK_ID }}"
          echo "   3. Digite a senha do artifact"
          echo "   4. Clique em Connect!"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "⏱️  Sessão ativa por ${{ env.SESSION_DURATION }} hora(s)"
          echo ""
          echo "🔒 SEGURANÇA: A senha NÃO é exibida nos logs."
          echo "   Apenas você pode baixar o artifact com as credenciais."
          echo ""

      - name: ⏱️ Manter Sessão Ativa
        run: |
          chmod +x scripts/keep-alive.sh
          
          export DURATION_HOURS="${{ env.SESSION_DURATION }}"
          ./scripts/keep-alive.sh
