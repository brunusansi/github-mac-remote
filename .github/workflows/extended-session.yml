name: 🔗 Extended Mac Session (Auto-Chain)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Duração por cadeia (horas)'
        required: true
        default: '6'
        type: choice
        options:
          - '4'
          - '5'
          - '6'
      max_chains:
        description: 'Número máximo de cadeias'
        required: true
        default: '2'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
      runner_size:
        description: 'Tamanho do runner'
        required: true
        default: 'standard'
        type: choice
        options:
          - 'standard'
          - 'large'
          - 'xlarge'
      current_chain:
        description: 'Cadeia atual (uso interno)'
        required: false
        default: '1'

env:
  SESSION_DURATION: ${{ github.event.inputs.duration }}
  MAX_CHAINS: ${{ github.event.inputs.max_chains }}
  CURRENT_CHAIN: ${{ github.event.inputs.current_chain }}

jobs:
  extended-session:
    name: 🔗 Sessão Estendida (Cadeia ${{ github.event.inputs.current_chain }}/${{ github.event.inputs.max_chains }})
    runs-on: ${{ github.event.inputs.runner_size == 'xlarge' && 'macos-14-xlarge' || github.event.inputs.runner_size == 'large' && 'macos-14-large' || 'macos-14' }}
    timeout-minutes: 375
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📊 Informações do Sistema
        run: |
          chmod +x scripts/system-info.sh
          ./scripts/system-info.sh
          
          echo ""
          echo "🔗 Informações da Cadeia:"
          echo "   ├─ Cadeia Atual: ${{ env.CURRENT_CHAIN }} / ${{ env.MAX_CHAINS }}"
          echo "   ├─ Duração: ${{ env.SESSION_DURATION }} horas"
          TOTAL_TIME=$(( ${{ env.CURRENT_CHAIN }} * ${{ env.SESSION_DURATION }} ))
          echo "   └─ Tempo Total (até agora): ${TOTAL_TIME} horas"
          echo ""

      - name: 🦀 Configurar RustDesk
        run: |
          chmod +x scripts/setup-rustdesk.sh
          
          # Gerar senha segura
          RUSTDESK_PASSWORD=$(openssl rand -base64 12 | tr -dc 'a-zA-Z0-9' | head -c 12)
          
          # Mascarar a senha nos logs do GitHub Actions
          echo "::add-mask::${RUSTDESK_PASSWORD}"
          
          # Salvar como variável de ambiente (mascarada)
          echo "RUSTDESK_PASSWORD=${RUSTDESK_PASSWORD}" >> $GITHUB_ENV
          
          export RUSTDESK_PASSWORD
          ./scripts/setup-rustdesk.sh
          
          # Aguardar RustDesk registrar
          sleep 10

      - name: 🔑 Obter RustDesk ID
        run: |
          sleep 5
          
          RUSTDESK_CLI="/Applications/RustDesk.app/Contents/MacOS/rustdesk"
          
          if [ -f "$RUSTDESK_CLI" ]; then
            RUSTDESK_ID=$("$RUSTDESK_CLI" --get-id 2>/dev/null || echo "")
            
            if [ -z "$RUSTDESK_ID" ]; then
              CONFIG_DIR="$HOME/Library/Application Support/RustDesk"
              RUSTDESK_ID=$(cat "${CONFIG_DIR}/id" 2>/dev/null || echo "")
            fi
            
            if [ -n "$RUSTDESK_ID" ]; then
              echo "RUSTDESK_ID=${RUSTDESK_ID}" >> $GITHUB_ENV
              echo "✅ RustDesk ID: ${RUSTDESK_ID}"
            else
              echo "RUSTDESK_ID=Ver_Logs" >> $GITHUB_ENV
            fi
          fi

      - name: 💾 Salvar Credenciais (Arquivo Privado)
        run: |
          mkdir -p credentials
          
          # Criar arquivo com credenciais (NÃO exibir no log)
          cat > credentials/connection-info.txt << EOF
          ════════════════════════════════════════════════════════════
            SESSÃO ESTENDIDA - Cadeia ${{ env.CURRENT_CHAIN }}/${{ env.MAX_CHAINS }}
          ════════════════════════════════════════════════════════════
          
          Iniciado por: ${{ github.actor }}
          Data/Hora: $(date -u '+%Y-%m-%d %H:%M:%S UTC')
          Run ID: ${{ github.run_id }}
          
          ────────────────────────────────────────────────────────────
          Credenciais RustDesk:
          ────────────────────────────────────────────────────────────
          
            ID:    ${{ env.RUSTDESK_ID }}
            Senha: ${{ env.RUSTDESK_PASSWORD }}
          
          ────────────────────────────────────────────────────────────
          Como Conectar:
          ────────────────────────────────────────────────────────────
          
          1. Baixe o RustDesk: https://rustdesk.com/download
          2. Digite o ID e senha acima
          3. Clique Connect!
          
          ⚠️  Nova cadeia = novas credenciais!
          
          ════════════════════════════════════════════════════════════
          ⚠️  ATENÇÃO: Este arquivo contém credenciais sensíveis.
              Não compartilhe com terceiros.
          ════════════════════════════════════════════════════════════
          EOF

      - name: 📦 Upload Credentials Artifact
        uses: actions/upload-artifact@v4
        with:
          # Nome do artifact inclui o usuário e cadeia para identificação
          name: credentials-${{ github.actor }}-chain${{ env.CURRENT_CHAIN }}-${{ github.run_id }}
          path: credentials/
          retention-days: 1

      - name: 📋 Instruções de Conexão
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════════════╗"
          echo "║   🔗 SESSÃO ESTENDIDA - Cadeia ${{ env.CURRENT_CHAIN }}/${{ env.MAX_CHAINS }}                          ║"
          echo "╚══════════════════════════════════════════════════════════════════╝"
          echo ""
          echo "👤 Sessão iniciada por: ${{ github.actor }}"
          echo ""
          echo "🔑 RustDesk ID: ${{ env.RUSTDESK_ID }}"
          echo ""
          echo "🔐 Senha: ████████████ (protegida)"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📥 COMO OBTER A SENHA:"
          echo ""
          echo "   1. Vá para a aba 'Summary' deste workflow"
          echo "   2. Baixe o artifact: credentials-${{ github.actor }}-chain${{ env.CURRENT_CHAIN }}-${{ github.run_id }}"
          echo "   3. Abra o arquivo connection-info.txt"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "🔒 SEGURANÇA: A senha NÃO é exibida nos logs."
          echo ""

      - name: ⏱️ Manter Sessão Ativa (com trigger de cadeia)
        run: |
          chmod +x scripts/keep-alive.sh
          
          DURATION_HOURS=${{ env.SESSION_DURATION }}
          KEEPALIVE_INTERVAL=300
          
          # Calcular tempos
          DURATION_SECONDS=$((DURATION_HOURS * 3600))
          CHAIN_TRIGGER_TIME=$((DURATION_SECONDS - 120)) # 2 minutos antes do fim
          START_TIME=$(date +%s)
          END_TIME=$((START_TIME + DURATION_SECONDS))
          
          echo "⏱️  Keep-alive iniciado"
          echo "   Duração: ${DURATION_HOURS} hora(s)"
          echo "   Trigger da próxima cadeia: 2 minutos antes do fim"
          echo ""
          
          triggered_chain=false
          
          while true; do
            CURRENT_TIME=$(date +%s)
            ELAPSED=$((CURRENT_TIME - START_TIME))
            REMAINING=$((END_TIME - CURRENT_TIME))
            
            if [ $REMAINING -le 0 ]; then
              echo ""
              echo "⏰ Tempo da sessão expirado."
              break
            fi
            
            # Disparar próxima cadeia 2 minutos antes do fim
            if [ $ELAPSED -ge $CHAIN_TRIGGER_TIME ] && [ "$triggered_chain" = false ]; then
              CURRENT_CHAIN=${{ env.CURRENT_CHAIN }}
              MAX_CHAINS=${{ env.MAX_CHAINS }}
              NEXT_CHAIN=$((CURRENT_CHAIN + 1))
              
              if [ $NEXT_CHAIN -le $MAX_CHAINS ]; then
                echo ""
                echo "🔗 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                echo "🔗 Iniciando próxima cadeia (${NEXT_CHAIN}/${MAX_CHAINS})..."
                echo "🔗 Nova sessão com novas credenciais em breve."
                echo "🔗 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                echo ""
                
                # Disparar próximo workflow
                gh workflow run extended-session.yml \
                  -f duration=${{ env.SESSION_DURATION }} \
                  -f max_chains=${{ env.MAX_CHAINS }} \
                  -f runner_size=${{ github.event.inputs.runner_size }} \
                  -f current_chain=${NEXT_CHAIN}
                
                triggered_chain=true
              else
                echo ""
                echo "⚠️  Máximo de cadeias atingido. Sem mais extensões."
              fi
            fi
            
            # Avisos
            if [ $REMAINING -le 900 ] && [ $REMAINING -gt 890 ]; then
              echo ""
              echo "⚠️  15 minutos restantes nesta cadeia!"
            fi
            
            if [ $REMAINING -le 300 ] && [ $REMAINING -gt 290 ]; then
              echo ""
              echo "🚨 5 minutos restantes! Verifique novas credenciais da próxima cadeia."
            fi
            
            # Heartbeat
            HOURS=$((REMAINING / 3600))
            MINUTES=$(((REMAINING % 3600) / 60))
            printf "\r💓 [%02d:%02d restante] Cadeia ${{ env.CURRENT_CHAIN }} ativa - $(date '+%H:%M:%S')     " $HOURS $MINUTES
            
            SLEEP_TIME=$((REMAINING < KEEPALIVE_INTERVAL ? REMAINING : KEEPALIVE_INTERVAL))
            sleep $SLEEP_TIME
          done
          
          echo ""
          echo "👋 Cadeia ${{ env.CURRENT_CHAIN }} finalizada."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
